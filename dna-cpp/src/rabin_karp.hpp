#ifndef RABIN_KARP_HPP
#define RABIN_KARP_HPP

#include <string>
#include <vector>

// Utiliza hashing para encontrar coincidencias.
std::vector<int> RabinKarpSearch(const std::string& text, const std::string& pattern);

#endif 
